generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Place {
  id          Int     @id @default(autoincrement())
  fk_place    Int
  node        Int
  name        String  @unique
  
  email       String
  website     String
  opening_hours String

  lat         Float
  long        Float
  addr_city   String
  addr_housenumber  Int
  addr_postcode   Int
  addr_Street String
  
  cuisine     String
  diet_vegan Boolean
  diet_vegetarian Boolean
}